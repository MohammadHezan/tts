# Core web / streaming
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
websockets>=13.0,<15.0

# Config / typing / env
pydantic>=2.8,<3.0
pyyaml>=6.0,<7.0
python-dotenv>=1.0,<2.0

# Audio I/O + numerics
numpy>=1.26,<3.0
soundfile>=0.12,<1.0

# VAD (ONNX runtime build of Silero VAD - no torch dependency, CPU-cheap)
silero-vad>=5.1
onnxruntime>=1.18,<2.0

# Streaming ASR (CTranslate2-backed Whisper, no torch dependency)
faster-whisper>=1.1,<2.0  # 1.1 added detect_language (asr.candidate_languages)

# Translator providers
httpx>=0.27,<1.0

# TTS: Kokoro for English (Apache-2.0, ONNX runtime - no torch), Piper for Arabic
# (GPL-3.0-or-later - the only well-maintained offline neural Arabic TTS with a
# real Python API we found; see README "Licensing" before shipping commercially).
# Both need voice model weights downloaded separately - see README "Setup".
kokoro-onnx>=0.6,<1.0
piper-tts>=1.8,<2.0
# Microsoft's neural voices (the ones Edge's Read Aloud uses; LGPL-3.0, no key,
# online) - tts.provider neural, with Kokoro/Piper above as the offline fallback.
edge-tts>=7.0,<8.0

# Live mic capture (CLI harness). Requires system libportaudio2 at runtime.
sounddevice>=0.4.6,<1.0

# Benchmarking
jiwer>=3.0,<5.0

# Tests
pytest>=8.0,<10.0
pytest-asyncio>=0.23,<2.0

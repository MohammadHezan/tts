// Intentionally empty: each module declares its own plugins + versions below,
// so :core (plain Kotlin/JVM) never triggers resolution of the Android Gradle
// Plugin, which :app needs but :core does not.

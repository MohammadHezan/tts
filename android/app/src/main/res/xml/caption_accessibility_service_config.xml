<?xml version="1.0" encoding="utf-8"?>
<!-- Watches on-screen text only in the two apps named below (Zoom, Google
     Meet) - never anything else on the phone. See CaptionAccessibilityService
     for what it does with that text: reads their own Live Captions feature's
     text, translates it, speaks the translation. It never touches audio,
     never sends anything anywhere, and never acts on the screen. -->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowContentChanged|typeViewTextChanged|typeWindowStateChanged"
    android:accessibilityFeedbackType="feedbackGeneric"
    android:canRetrieveWindowContent="true"
    android:description="@string/caption_bridge_service_description"
    android:notificationTimeout="150"
    android:packageNames="us.zoom.videomeetings,com.google.android.apps.meetings" />
